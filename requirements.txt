# ===== Phase 1 依赖清单 =====

# Web 框架 + ASGI 服务
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# 配置与数据校验
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# 数据库（PostgreSQL + SQLAlchemy 异步）
sqlalchemy>=2.0.25
asyncpg>=0.29.0
greenlet>=3.0.3          # SQLAlchemy 异步模式依赖
alembic>=1.13.0          # 数据库版本迁移（Phase 2 起正式使用）

# HTTP 客户端（异步调用高德地图 API）
httpx>=0.27.0

# ---- 可选（Phase 1 可先不装，后续再补） ----
# python-jose[cryptography]   # JWT 令牌（如需更正式的登录态）
# ortools                      # Phase 2 路线规划
# pgvector                     # Phase 3 语义检索
